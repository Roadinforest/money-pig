// Pure computation for the Accounts tab tile + bar chart.
// Re-exported from monthlyStats for convenience; kept here so the Accounts
// feature does not depend on the Stats module's naming.

export { buildAccountStats, type AccountStats, type AccountChartEntry } from "../stats/monthlyStats";
